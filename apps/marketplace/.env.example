# Production/preview: a Neon connection string. Never committed — see
# CONTRIBUTING.md's "Marketplace: local Postgres" section for the
# no-Neon-access local contributor path (docker/hub-db.yml).
DATABASE_URL=

# Shared secret the publish CI workflow sends as PublishRequest.apiKey.
# Optional locally — publishing is unauthenticated when unset (dev only).
HUB_PUBLISH_KEY=
