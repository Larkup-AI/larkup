---
"@larkup/cli": patch
---

`larkup update`: show bold "Already up to date" banner when CLI is current; use a spinner with black text during npm install; show bold success banner after a successful update.
