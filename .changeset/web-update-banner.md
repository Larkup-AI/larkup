---
"larkup": patch
---

`larkup update`: check version before running npm install — shows bold "Already up to date" banner when current, dim gray text during install, and bold success banner after update. Stops re-running npm install when nothing to upgrade.
